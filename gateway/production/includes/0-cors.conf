map $http_origin $cors_origin_header {
    default "";
    "~^https:\/\/(misadmin|www).misakey.com$" "$http_origin";
}

map $http_origin $cors_cred {
    default "";
    "~^https:\/\/(misadmin|www).misakey.com$" "true";
}
