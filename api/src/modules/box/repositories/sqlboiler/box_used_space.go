// Code generated by SQLBoiler 4.2.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package sqlboiler

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BoxUsedSpace is an object representing the database table.
type BoxUsedSpace struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	BoxID     string    `boil:"box_id" json:"box_id" toml:"box_id" yaml:"box_id"`
	Value     int64     `boil:"value" json:"value" toml:"value" yaml:"value"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *boxUsedSpaceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L boxUsedSpaceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BoxUsedSpaceColumns = struct {
	ID        string
	BoxID     string
	Value     string
	CreatedAt string
}{
	ID:        "id",
	BoxID:     "box_id",
	Value:     "value",
	CreatedAt: "created_at",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var BoxUsedSpaceWhere = struct {
	ID        whereHelperstring
	BoxID     whereHelperstring
	Value     whereHelperint64
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperstring{field: "\"box_used_space\".\"id\""},
	BoxID:     whereHelperstring{field: "\"box_used_space\".\"box_id\""},
	Value:     whereHelperint64{field: "\"box_used_space\".\"value\""},
	CreatedAt: whereHelpertime_Time{field: "\"box_used_space\".\"created_at\""},
}

// BoxUsedSpaceRels is where relationship names are stored.
var BoxUsedSpaceRels = struct {
}{}

// boxUsedSpaceR is where relationships are stored.
type boxUsedSpaceR struct {
}

// NewStruct creates a new relationship struct
func (*boxUsedSpaceR) NewStruct() *boxUsedSpaceR {
	return &boxUsedSpaceR{}
}

// boxUsedSpaceL is where Load methods for each relationship are stored.
type boxUsedSpaceL struct{}

var (
	boxUsedSpaceAllColumns            = []string{"id", "box_id", "value", "created_at"}
	boxUsedSpaceColumnsWithoutDefault = []string{"id", "box_id", "value"}
	boxUsedSpaceColumnsWithDefault    = []string{"created_at"}
	boxUsedSpacePrimaryKeyColumns     = []string{"id"}
)

type (
	// BoxUsedSpaceSlice is an alias for a slice of pointers to BoxUsedSpace.
	// This should generally be used opposed to []BoxUsedSpace.
	BoxUsedSpaceSlice []*BoxUsedSpace

	boxUsedSpaceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	boxUsedSpaceType                 = reflect.TypeOf(&BoxUsedSpace{})
	boxUsedSpaceMapping              = queries.MakeStructMapping(boxUsedSpaceType)
	boxUsedSpacePrimaryKeyMapping, _ = queries.BindMapping(boxUsedSpaceType, boxUsedSpaceMapping, boxUsedSpacePrimaryKeyColumns)
	boxUsedSpaceInsertCacheMut       sync.RWMutex
	boxUsedSpaceInsertCache          = make(map[string]insertCache)
	boxUsedSpaceUpdateCacheMut       sync.RWMutex
	boxUsedSpaceUpdateCache          = make(map[string]updateCache)
	boxUsedSpaceUpsertCacheMut       sync.RWMutex
	boxUsedSpaceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single boxUsedSpace record from the query.
func (q boxUsedSpaceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BoxUsedSpace, error) {
	o := &BoxUsedSpace{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "sqlboiler: failed to execute a one query for box_used_space")
	}

	return o, nil
}

// All returns all BoxUsedSpace records from the query.
func (q boxUsedSpaceQuery) All(ctx context.Context, exec boil.ContextExecutor) (BoxUsedSpaceSlice, error) {
	var o []*BoxUsedSpace

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "sqlboiler: failed to assign all query results to BoxUsedSpace slice")
	}

	return o, nil
}

// Count returns the count of all BoxUsedSpace records in the query.
func (q boxUsedSpaceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to count box_used_space rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q boxUsedSpaceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "sqlboiler: failed to check if box_used_space exists")
	}

	return count > 0, nil
}

// BoxUsedSpaces retrieves all the records using an executor.
func BoxUsedSpaces(mods ...qm.QueryMod) boxUsedSpaceQuery {
	mods = append(mods, qm.From("\"box_used_space\""))
	return boxUsedSpaceQuery{NewQuery(mods...)}
}

// FindBoxUsedSpace retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBoxUsedSpace(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*BoxUsedSpace, error) {
	boxUsedSpaceObj := &BoxUsedSpace{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"box_used_space\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, boxUsedSpaceObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "sqlboiler: unable to select from box_used_space")
	}

	return boxUsedSpaceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BoxUsedSpace) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("sqlboiler: no box_used_space provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(boxUsedSpaceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	boxUsedSpaceInsertCacheMut.RLock()
	cache, cached := boxUsedSpaceInsertCache[key]
	boxUsedSpaceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			boxUsedSpaceAllColumns,
			boxUsedSpaceColumnsWithDefault,
			boxUsedSpaceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(boxUsedSpaceType, boxUsedSpaceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(boxUsedSpaceType, boxUsedSpaceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"box_used_space\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"box_used_space\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "sqlboiler: unable to insert into box_used_space")
	}

	if !cached {
		boxUsedSpaceInsertCacheMut.Lock()
		boxUsedSpaceInsertCache[key] = cache
		boxUsedSpaceInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the BoxUsedSpace.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BoxUsedSpace) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	boxUsedSpaceUpdateCacheMut.RLock()
	cache, cached := boxUsedSpaceUpdateCache[key]
	boxUsedSpaceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			boxUsedSpaceAllColumns,
			boxUsedSpacePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("sqlboiler: unable to update box_used_space, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"box_used_space\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, boxUsedSpacePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(boxUsedSpaceType, boxUsedSpaceMapping, append(wl, boxUsedSpacePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to update box_used_space row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by update for box_used_space")
	}

	if !cached {
		boxUsedSpaceUpdateCacheMut.Lock()
		boxUsedSpaceUpdateCache[key] = cache
		boxUsedSpaceUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q boxUsedSpaceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to update all for box_used_space")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to retrieve rows affected for box_used_space")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BoxUsedSpaceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("sqlboiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), boxUsedSpacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"box_used_space\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, boxUsedSpacePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to update all in boxUsedSpace slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to retrieve rows affected all in update all boxUsedSpace")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BoxUsedSpace) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("sqlboiler: no box_used_space provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(boxUsedSpaceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	boxUsedSpaceUpsertCacheMut.RLock()
	cache, cached := boxUsedSpaceUpsertCache[key]
	boxUsedSpaceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			boxUsedSpaceAllColumns,
			boxUsedSpaceColumnsWithDefault,
			boxUsedSpaceColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			boxUsedSpaceAllColumns,
			boxUsedSpacePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("sqlboiler: unable to upsert box_used_space, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(boxUsedSpacePrimaryKeyColumns))
			copy(conflict, boxUsedSpacePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"box_used_space\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(boxUsedSpaceType, boxUsedSpaceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(boxUsedSpaceType, boxUsedSpaceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "sqlboiler: unable to upsert box_used_space")
	}

	if !cached {
		boxUsedSpaceUpsertCacheMut.Lock()
		boxUsedSpaceUpsertCache[key] = cache
		boxUsedSpaceUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single BoxUsedSpace record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BoxUsedSpace) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("sqlboiler: no BoxUsedSpace provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), boxUsedSpacePrimaryKeyMapping)
	sql := "DELETE FROM \"box_used_space\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to delete from box_used_space")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by delete for box_used_space")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q boxUsedSpaceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("sqlboiler: no boxUsedSpaceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to delete all from box_used_space")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by deleteall for box_used_space")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BoxUsedSpaceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), boxUsedSpacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"box_used_space\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, boxUsedSpacePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to delete all from boxUsedSpace slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by deleteall for box_used_space")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BoxUsedSpace) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBoxUsedSpace(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BoxUsedSpaceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BoxUsedSpaceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), boxUsedSpacePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"box_used_space\".* FROM \"box_used_space\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, boxUsedSpacePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "sqlboiler: unable to reload all in BoxUsedSpaceSlice")
	}

	*o = slice

	return nil
}

// BoxUsedSpaceExists checks if the BoxUsedSpace row exists.
func BoxUsedSpaceExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"box_used_space\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "sqlboiler: unable to check if box_used_space exists")
	}

	return exists, nil
}
