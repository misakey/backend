// Code generated by SQLBoiler 3.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package sqlboiler

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// EncryptedFile is an object representing the database table.
type EncryptedFile struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	Size      int64     `boil:"size" json:"size" toml:"size" yaml:"size"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *encryptedFileR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L encryptedFileL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EncryptedFileColumns = struct {
	ID        string
	Size      string
	CreatedAt string
}{
	ID:        "id",
	Size:      "size",
	CreatedAt: "created_at",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}

var EncryptedFileWhere = struct {
	ID        whereHelperstring
	Size      whereHelperint64
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperstring{field: "\"encrypted_file\".\"id\""},
	Size:      whereHelperint64{field: "\"encrypted_file\".\"size\""},
	CreatedAt: whereHelpertime_Time{field: "\"encrypted_file\".\"created_at\""},
}

// EncryptedFileRels is where relationship names are stored.
var EncryptedFileRels = struct {
	SavedFiles string
}{
	SavedFiles: "SavedFiles",
}

// encryptedFileR is where relationships are stored.
type encryptedFileR struct {
	SavedFiles SavedFileSlice
}

// NewStruct creates a new relationship struct
func (*encryptedFileR) NewStruct() *encryptedFileR {
	return &encryptedFileR{}
}

// encryptedFileL is where Load methods for each relationship are stored.
type encryptedFileL struct{}

var (
	encryptedFileAllColumns            = []string{"id", "size", "created_at"}
	encryptedFileColumnsWithoutDefault = []string{"id", "size", "created_at"}
	encryptedFileColumnsWithDefault    = []string{}
	encryptedFilePrimaryKeyColumns     = []string{"id"}
)

type (
	// EncryptedFileSlice is an alias for a slice of pointers to EncryptedFile.
	// This should generally be used opposed to []EncryptedFile.
	EncryptedFileSlice []*EncryptedFile

	encryptedFileQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	encryptedFileType                 = reflect.TypeOf(&EncryptedFile{})
	encryptedFileMapping              = queries.MakeStructMapping(encryptedFileType)
	encryptedFilePrimaryKeyMapping, _ = queries.BindMapping(encryptedFileType, encryptedFileMapping, encryptedFilePrimaryKeyColumns)
	encryptedFileInsertCacheMut       sync.RWMutex
	encryptedFileInsertCache          = make(map[string]insertCache)
	encryptedFileUpdateCacheMut       sync.RWMutex
	encryptedFileUpdateCache          = make(map[string]updateCache)
	encryptedFileUpsertCacheMut       sync.RWMutex
	encryptedFileUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single encryptedFile record from the query.
func (q encryptedFileQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EncryptedFile, error) {
	o := &EncryptedFile{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "sqlboiler: failed to execute a one query for encrypted_file")
	}

	return o, nil
}

// All returns all EncryptedFile records from the query.
func (q encryptedFileQuery) All(ctx context.Context, exec boil.ContextExecutor) (EncryptedFileSlice, error) {
	var o []*EncryptedFile

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "sqlboiler: failed to assign all query results to EncryptedFile slice")
	}

	return o, nil
}

// Count returns the count of all EncryptedFile records in the query.
func (q encryptedFileQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to count encrypted_file rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q encryptedFileQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "sqlboiler: failed to check if encrypted_file exists")
	}

	return count > 0, nil
}

// SavedFiles retrieves all the saved_file's SavedFiles with an executor.
func (o *EncryptedFile) SavedFiles(mods ...qm.QueryMod) savedFileQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"saved_file\".\"encrypted_file_id\"=?", o.ID),
	)

	query := SavedFiles(queryMods...)
	queries.SetFrom(query.Query, "\"saved_file\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"saved_file\".*"})
	}

	return query
}

// LoadSavedFiles allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (encryptedFileL) LoadSavedFiles(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEncryptedFile interface{}, mods queries.Applicator) error {
	var slice []*EncryptedFile
	var object *EncryptedFile

	if singular {
		object = maybeEncryptedFile.(*EncryptedFile)
	} else {
		slice = *maybeEncryptedFile.(*[]*EncryptedFile)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &encryptedFileR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &encryptedFileR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`saved_file`), qm.WhereIn(`saved_file.encrypted_file_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load saved_file")
	}

	var resultSlice []*SavedFile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice saved_file")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on saved_file")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for saved_file")
	}

	if singular {
		object.R.SavedFiles = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &savedFileR{}
			}
			foreign.R.EncryptedFile = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.EncryptedFileID {
				local.R.SavedFiles = append(local.R.SavedFiles, foreign)
				if foreign.R == nil {
					foreign.R = &savedFileR{}
				}
				foreign.R.EncryptedFile = local
				break
			}
		}
	}

	return nil
}

// AddSavedFiles adds the given related objects to the existing relationships
// of the encrypted_file, optionally inserting them as new records.
// Appends related to o.R.SavedFiles.
// Sets related.R.EncryptedFile appropriately.
func (o *EncryptedFile) AddSavedFiles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*SavedFile) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.EncryptedFileID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"saved_file\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"encrypted_file_id"}),
				strmangle.WhereClause("\"", "\"", 2, savedFilePrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.EncryptedFileID = o.ID
		}
	}

	if o.R == nil {
		o.R = &encryptedFileR{
			SavedFiles: related,
		}
	} else {
		o.R.SavedFiles = append(o.R.SavedFiles, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &savedFileR{
				EncryptedFile: o,
			}
		} else {
			rel.R.EncryptedFile = o
		}
	}
	return nil
}

// EncryptedFiles retrieves all the records using an executor.
func EncryptedFiles(mods ...qm.QueryMod) encryptedFileQuery {
	mods = append(mods, qm.From("\"encrypted_file\""))
	return encryptedFileQuery{NewQuery(mods...)}
}

// FindEncryptedFile retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEncryptedFile(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*EncryptedFile, error) {
	encryptedFileObj := &EncryptedFile{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"encrypted_file\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, encryptedFileObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "sqlboiler: unable to select from encrypted_file")
	}

	return encryptedFileObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EncryptedFile) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("sqlboiler: no encrypted_file provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(encryptedFileColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	encryptedFileInsertCacheMut.RLock()
	cache, cached := encryptedFileInsertCache[key]
	encryptedFileInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			encryptedFileAllColumns,
			encryptedFileColumnsWithDefault,
			encryptedFileColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(encryptedFileType, encryptedFileMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(encryptedFileType, encryptedFileMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"encrypted_file\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"encrypted_file\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "sqlboiler: unable to insert into encrypted_file")
	}

	if !cached {
		encryptedFileInsertCacheMut.Lock()
		encryptedFileInsertCache[key] = cache
		encryptedFileInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the EncryptedFile.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EncryptedFile) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	encryptedFileUpdateCacheMut.RLock()
	cache, cached := encryptedFileUpdateCache[key]
	encryptedFileUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			encryptedFileAllColumns,
			encryptedFilePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("sqlboiler: unable to update encrypted_file, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"encrypted_file\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, encryptedFilePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(encryptedFileType, encryptedFileMapping, append(wl, encryptedFilePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to update encrypted_file row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by update for encrypted_file")
	}

	if !cached {
		encryptedFileUpdateCacheMut.Lock()
		encryptedFileUpdateCache[key] = cache
		encryptedFileUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q encryptedFileQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to update all for encrypted_file")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to retrieve rows affected for encrypted_file")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EncryptedFileSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("sqlboiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), encryptedFilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"encrypted_file\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, encryptedFilePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to update all in encryptedFile slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to retrieve rows affected all in update all encryptedFile")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EncryptedFile) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("sqlboiler: no encrypted_file provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(encryptedFileColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	encryptedFileUpsertCacheMut.RLock()
	cache, cached := encryptedFileUpsertCache[key]
	encryptedFileUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			encryptedFileAllColumns,
			encryptedFileColumnsWithDefault,
			encryptedFileColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			encryptedFileAllColumns,
			encryptedFilePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("sqlboiler: unable to upsert encrypted_file, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(encryptedFilePrimaryKeyColumns))
			copy(conflict, encryptedFilePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"encrypted_file\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(encryptedFileType, encryptedFileMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(encryptedFileType, encryptedFileMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "sqlboiler: unable to upsert encrypted_file")
	}

	if !cached {
		encryptedFileUpsertCacheMut.Lock()
		encryptedFileUpsertCache[key] = cache
		encryptedFileUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single EncryptedFile record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EncryptedFile) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("sqlboiler: no EncryptedFile provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), encryptedFilePrimaryKeyMapping)
	sql := "DELETE FROM \"encrypted_file\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to delete from encrypted_file")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by delete for encrypted_file")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q encryptedFileQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("sqlboiler: no encryptedFileQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to delete all from encrypted_file")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by deleteall for encrypted_file")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EncryptedFileSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), encryptedFilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"encrypted_file\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, encryptedFilePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: unable to delete all from encryptedFile slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "sqlboiler: failed to get rows affected by deleteall for encrypted_file")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EncryptedFile) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEncryptedFile(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EncryptedFileSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EncryptedFileSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), encryptedFilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"encrypted_file\".* FROM \"encrypted_file\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, encryptedFilePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "sqlboiler: unable to reload all in EncryptedFileSlice")
	}

	*o = slice

	return nil
}

// EncryptedFileExists checks if the EncryptedFile row exists.
func EncryptedFileExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"encrypted_file\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "sqlboiler: unable to check if encrypted_file exists")
	}

	return exists, nil
}
